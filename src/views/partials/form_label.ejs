<% if (!labelInfo) { %>
  <% throw new Error("Must provide 'labelInfo' object") %>
<% } %>

<label 
  for="<%= labelInfo.for %>"
  class="
    block 
    text-sm 
    font-medium 
    text-gray-700 
    mt-4  
  "
>
  <%= labelInfo.text %>
</label>
